{
  "name": "orgwide-branches",
  "version": "1.0.0",
  "description": "GitHub Action to list repositories and search for branches across an organization",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/snsinahub/orgwide-branches.git"
  },
  "keywords": [
    "github",
    "action",
    "branches",
    "organization",
    "repositories"
  ],
  "author": "Siavash Namvar",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
